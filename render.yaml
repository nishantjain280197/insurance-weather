services:
  - type: web
    name: weathershield
    runtime: node
    plan: free
    nodeVersion: 20.18.0
    buildCommand: npm run install:all && npm run build
    startCommand: NODE_ENV=production npm start
    envVars:
      - key: JWT_SECRET
        generateValue: true
